package enums.first;

public enum NoValues { //It's possible to create a enum with no values and no variable references
    ;
    private String value;//If instance variables are added, it's necessary to conclude the values area with a semicolon, othwerise it will not compile
}
